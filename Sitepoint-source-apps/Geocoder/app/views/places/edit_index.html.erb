<h1>Edit Places</h1>

<div class="card">
  <div class="card-block">
    <ul>
    	<% @places.each do |place| %>
		    <li>
			  <%= link_to place.title, edit_place_path(place) %>
			  visited by <strong><%= place.visited_by %></strong>
			</li>
		<% end %>
    </ul>
  </div>
</div>

